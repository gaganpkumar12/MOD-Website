# Static build submits directly to PHP proxy in the exported files.
# Keep this empty to use /early-access-proxy.php on the same domain.
# Or set a full URL to a different endpoint.
NEXT_PUBLIC_EARLY_ACCESS_ENDPOINT=
